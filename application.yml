foo: bar
bar: foo
baz: 123
greeting: Hi there!
# greeting.message: Hello Default Profile!

---
spring:
  config:
    activate:
      on-profile: cloud

greeting.message: Hello Cloud Profile (where profile is determined by spring.config.activate.on-profile) !!!

baby:
  yoda:
    name: Grogu
